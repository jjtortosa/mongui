extends db

block dbbody
	table.lcaption
		thead
			tr
				th(colspan="3")!="admin.currentOp ( {ns: /^"+dbname+"./} )"
		tbody
			each i in processlist
				tr
					td: pre=JSON.stringify(i, null, '\t')