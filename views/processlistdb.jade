extends db

block dbbody
	table.lcaption
		thead
			tr
				th(colspan="3")!="currentOp() (db.$cmd.sys.inprog.find({ns: /^"+dbname+"./})"
		tbody
			each i in processlist
				tr
					td: pre=JSON.stringify(i, null, '\t')