extends layout

block content
	table.lcaption
		thead
			tr
				th(colspan="3") Replication (replSetGetStatus())
		tbody
			each i,k in info
				tr
					th=k
					td: pre=JSON.stringify(i, null, '\t')