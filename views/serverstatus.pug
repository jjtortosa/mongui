extends layout

block content
	.auto-height
		table.lcaption
			thead
				tr
					th(colspan="3") Estado del servidor ({serverStatus:1})
			tbody
				each i,k in info
					tr
						th=k
						td: pre=JSON.stringify(i, null, '\t')